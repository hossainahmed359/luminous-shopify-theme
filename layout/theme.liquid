<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    {% comment %} <link href="{{ 'style.css' | asset_url }}" rel="stylesheet"> {% endcomment %}
    {{ content_for_header }}
</head>
<body>
    {% section 'header' %}
    {{ content_for_layout }}
    {% section 'footer' %}

    {% comment %} <script src="{{ 'main.js' | asset_url }}" defer></script> {% endcomment %}
</body>
</html>